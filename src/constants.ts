export const ADDON_ID = "version-selector";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = `version-selector`;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
